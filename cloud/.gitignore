# Secrets and rendered artifacts — never commit.
secrets.env
cloud-init.rendered.yaml
**/cloud-init.rendered.yaml

# Local campaign state (lives in the decentr-results repo instead).
campaign-state/

# Generated pool (regenerated by terraform apply; keep pool.yaml.example tracked).
pool.yaml

# Terraform.
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.*
**/terraform.tfvars
**/*.auto.tfvars
**/crash.log

# Python.
__pycache__/
*.pyc
